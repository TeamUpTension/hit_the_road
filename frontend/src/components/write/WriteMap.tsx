import styled from "styled-components";

const WriteMap = () => {
    return (<Map />)
};

const Map = styled.div`
    width: 100%;
    height: 300px;
    background: lightgray;
`;

export default WriteMap;